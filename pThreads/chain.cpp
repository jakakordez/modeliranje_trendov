#include "stdafx.h"
#include <math.h>

#include "chain.h"
#include "matrix.h"

int fillMatrix(float * matrix, int *data, int n) {
	// parallel stuff
	return 1;
}

void normalizeMatrix(float *matrix) {
	// parallel stuff
}

